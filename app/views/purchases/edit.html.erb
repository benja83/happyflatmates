<%= form_for [@flat, @purchase],html: {class: "navbar-form navbar-right reset-margin", role: "form"} do |f| %>
    <%= f.hidden_field :flat_id, :value => @flat.id %>
    <%= f.hidden_field :pending, :value => false %>
    <%= f.submit "I bought it", class: "form-control"  %>
<% end %>